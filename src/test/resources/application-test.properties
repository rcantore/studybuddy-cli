# Test Configuration
spring.main.web-application-type=none
spring.main.banner-mode=off
spring.autoconfigure.exclude=com.embabel.agent.config.AgentPlatformConfiguration

# Disable AI configurations
spring.ai.model.embedding=none

# Logging - minimize output for tests
logging.level.org.springframework=WARN
logging.level.com.embabel=WARN
logging.level.root=WARN

# Language Configuration
studybuddy.default-locale=es

# Mock Anthropic API key for tests
anthropic.api.key=sk-test-key